/*
const Device = require('../models/deviceInstance')

const currentDevice = {
	_id: 1,
}

module.exports = {
	isDevice(req, res, next) {
		const post = Device.func(req.params.id)
		if() {
		  //next()
		} else {
			//req.session.error = 'Must be admin to edit!'
			//return res.redirect('/')
		}
	}
}

*/